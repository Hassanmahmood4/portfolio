/**
 * Root R3F Canvas has no default scene content (character & tech use their own canvases).
 * Keeps <Canvas /> + View.Port mount point for any future drei Views.
 */
export function CanvasBaseScene() {
  return null;
}
